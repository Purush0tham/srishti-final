# 🛡 CareGuard AI — Frontend

React + Vite frontend for the CareGuard AI caregiver support system.

## Run

```bash
npm install
npm run dev
# → http://localhost:5173
```

> Backend must be running on `http://localhost:3001`
> See `../backend/.env` for configuration.
